# Content Improvement Notes

This file tracks small follow-up opportunities found during five-pack audits.
It is intentionally lightweight so we can keep shipping narrow curriculum slices and still preserve later cleanup ideas.

## Current Watchlist

- Reading reuse:
  - Packs `39-40`, `45`, and `48-50` are now the thinnest late-curriculum pockets at `20%` reuse after the full Block E follow-up.
  - Packs `41-47` are no longer the immediate watchpoint; the Block E follow-up lifted them to `27-33%`.
  - The next global rebalance pass should start by deciding whether to deepen late-pack reuse first or tackle the older low-reuse clusters below `20%`.

- Historical low-reuse packs outside the newest block:
  - Packs `6-7`, `9-12`, `16-17`, and `22` remain relatively light on reading reuse and are good candidates for a later global rebalance pass after Block E is complete.

- Quality process:
  - Add new audit findings here whenever a pass surfaces a naturalness concern, a weak-reuse pocket, or a pack that has become too template-heavy.

# Content Improvement Notes

This file tracks small follow-up opportunities found during five-pack audits.
It is intentionally lightweight so we can keep shipping narrow curriculum slices and still preserve later cleanup ideas.

## Current Watchlist

- Reading reuse:
  - Completed: Global Reading-Reuse Cleanup Pass 1 lifted packs `6-12` from `14-17%` reuse to `25-33%` using only existing example sentences, and both new missions stayed at `20%` source-pack concentration.
  - Priority 1: follow with a coordination/calendar cleanup across packs `16-17` and `22`, with supporting reuse from adjacent already-shipped packs `18-20` and `23`; these are now the weakest active pockets at `13-14%`.
  - Priority 2: come back to the late-curriculum thin pockets at `20%` reuse in packs `39-40`, `45`, and `48-50` once the older low-density clusters stop dominating the audit.
  - Priority 3: if the claim gate still needs more reading density after those passes, target the next-lowest cluster at `20-21%` reuse across packs `14-15`, `21`, `25`, and `33-35`.
  - Packs `41-47` are no longer the immediate watchpoint; the Block E follow-up lifted them to `27-33%`.

- Historical low-reuse packs outside the newest block:
  - Packs `16-17` and `22` are now the strongest immediate rebalance candidates, with packs `39-40`, `45`, and `48-50` still the thinnest late-block follow-up set after that.

- Claim-gate remediation:
  - The repo is still `28` vocab items short of the repo-defined `550` floor, so vocabulary closure should be paired with or immediately follow the second global reading-reuse cleanup pass rather than being deferred indefinitely.

- Quality process:
  - Add new audit findings here whenever a pass surfaces a naturalness concern, a weak-reuse pocket, or a pack that has become too template-heavy.

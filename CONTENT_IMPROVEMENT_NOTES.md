# Content Improvement Notes

This file tracks small follow-up opportunities found during five-pack audits.
It is intentionally lightweight so we can keep shipping narrow curriculum slices and still preserve later cleanup ideas.

## Current Watchlist

- Reading reuse:
  - Completed: Global Reading-Reuse Cleanup Pass 1 lifted packs `6-12` from `14-17%` reuse to `25-33%` using only existing example sentences, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 2 lifted packs `16-17` and `22` from `13-14%` reuse to `25-29%` using only existing example sentences plus balanced support from packs `18-20` and `23`, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 3 lifted packs `39-40` and `45` from `20%` reuse to `33%` using only existing example sentences plus balanced support from packs `41-44`, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 4 lifted packs `48-50` from `20%` reuse to `33%` using only existing example sentences plus balanced support from packs `46-47`, and both new missions stayed at `20%` source-pack concentration while packs `46-47` landed exactly at the `40%` cap.
  - Completed: Global Reading-Reuse Cleanup Pass 5 lifted packs `21` and `25` from `20%` reuse to `33%` using only existing example sentences plus balanced support from packs `14-15` and `26-27`, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 6 lifted pack `8` and packs `33-35` from `21%` reuse to `36%` using only existing example sentences plus balanced support from nearby adjective/comparison material, and both new missions stayed at `20%` source-pack concentration.
  - Priority 1: move to vocab-floor closure and then rerun the claim gate, because the clearly thin reading-reuse floor has now been removed.
  - Priority 2: if the claim gate still wants more reading density after vocab closure, target the new weakest mid-density pocket led by pack `31` and the `25%` cluster across packs `9`, `11`, `13`, and `17`.
  - Priority 3: once the claim gate is rerun, decide whether any broader mid-density polish is actually worth doing or whether Phase 3 should close and the focus should shift to the documented product/UX backlog.
  - Packs `8`, `14-15`, `21`, `25`, `26-27`, `33-35`, and `46-50` are no longer watchpoints after the completed cleanup passes.

- Historical low-reuse packs outside the newest block:
  - The new weakest reuse pocket is pack `31` at `24%`, with the next tier at `25%` across packs `9`, `11`, `13`, and `17`.

- Claim-gate remediation:
  - The repo is still `28` vocab items short of the repo-defined `550` floor, and the visibly thin reading-reuse floor is now gone, so vocabulary closure is the next major remediation step before the next serious claim-gate rerun.

- Quality process:
  - Add new audit findings here whenever a pass surfaces a naturalness concern, a weak-reuse pocket, or a pack that has become too template-heavy.

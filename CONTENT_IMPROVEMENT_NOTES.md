# Content Improvement Notes

This file tracks small follow-up opportunities found during five-pack audits.
It is intentionally lightweight so we can keep shipping narrow curriculum slices and still preserve later cleanup ideas.

## Current Watchlist

- Reading reuse:
  - Completed: Global Reading-Reuse Cleanup Pass 1 lifted packs `6-12` from `14-17%` reuse to `25-33%` using only existing example sentences, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 2 lifted packs `16-17` and `22` from `13-14%` reuse to `25-29%` using only existing example sentences plus balanced support from packs `18-20` and `23`, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 3 lifted packs `39-40` and `45` from `20%` reuse to `33%` using only existing example sentences plus balanced support from packs `41-44`, and both new missions stayed at `20%` source-pack concentration.
  - Completed: Global Reading-Reuse Cleanup Pass 4 lifted packs `48-50` from `20%` reuse to `33%` using only existing example sentences plus balanced support from packs `46-47`, and both new missions stayed at `20%` source-pack concentration while packs `46-47` landed exactly at the `40%` cap.
  - Priority 1: target the last `20%` floor at packs `21` and `25`, ideally with balanced support from packs `14-15` and `26-27`, so there are no clearly under-reused pockets left before another claim-gate rerun.
  - Priority 2: if more reading density is still warranted after that, target the next `21%` cluster across packs `8`, `14-15`, `26-27`, and `33-35`.
  - Priority 3: once the thin-pack pockets are gone, decide whether the better claim-gate move is vocab-floor closure or one broader polish pass that lifts the remaining `24-29%` middle tier.
  - Packs `46-50` are no longer a watchpoint; the late-tail cleanup lifted the full end block to `33-40%`.

- Historical low-reuse packs outside the newest block:
  - Packs `21` and `25` now form the lowest remaining reuse floor at `20%`, with packs `8`, `14-15`, `26-27`, and `33-35` forming the next mixed-density cleanup tier at `21%`.

- Claim-gate remediation:
  - The repo is still `28` vocab items short of the repo-defined `550` floor, so vocabulary closure should be paired with or follow the next cleanup decision rather than being deferred indefinitely now that the late-block reading tail is fixed.

- Quality process:
  - Add new audit findings here whenever a pass surfaces a naturalness concern, a weak-reuse pocket, or a pack that has become too template-heavy.
